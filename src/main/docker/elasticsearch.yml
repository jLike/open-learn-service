version: '2'
services:
    openlearn-elasticsearch:
        image: elasticsearch:2.4.1
        # volumes:
        #     - ~/volumes/jbooter/openlearn/elasticsearch/:/usr/share/elasticsearch/data/
        ports:
            - 9200:9200
            - 9300:9300
